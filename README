Description
	Agentsim is a project to create an integrated visualization environment for
	biological systems. The goal is to use various simulation approaches
	to represent the myriad phenomena that occur in a cell. Currently, ReaDDy
	is being used for Molecular Dynamics.

Dependencies
	- blosc
	- hdf5
	- blas
	- lapack

Building:
1. git clone --recursive [agentsim_repo_address]
1. Alternatively, after cloning the repository, run "git submodule update --init --recursive" to download dependencies.
2. Create a new directory for the build, navigate to it, and run
	"cmake [path to repository] -DBUILD_ONLY="s3;awstransfer;transfer" -DCMAKE_BUILD_TYPE=Release "
3. Run "make"

To create the docker image:
sudo docker build -t agentsim-dev .

To run the docker image:
sudo docker run -it -p 9002:9002 agentsim-dev:latest agentsim_server.exe
